# Simple In-Memory Key-Value Store

## Project Description

This project implements a simple **In-Memory Key-Value Store** backend service.
Data is stored temporarily in memory using a dictionary structure (Key → Value).
The service provides RESTful APIs to store and retrieve values by key.

---

## Technologies Used

- Python 3
- FastAPI
- Git (Version Control)
- Docker
